// Placeholder widget test.
//
// The default counter smoke test was removed because MyApp requires
// Firebase.initializeApp() which is unavailable in the test environment.
// Meaningful widget and unit tests live in test/models/, test/services/,
// and test/screens/.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — see test/models, test/services, test/screens', () {
    expect(true, isTrue);
  });
}
